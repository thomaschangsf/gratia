package functionalDesign.fd_wk0.example.src.test.scala.example

import personalCode.test

@RunWith(classOf[JUnitRunner])
class ListsSuite extends FunSuite {

  test("one plus one is two") {
    assert(1 + 1 == 2)
  }


}